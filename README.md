# Image-Generator-app
A lightweight Flask web server is used as the backend API to process user prompts and generate images. To make the local server publicly accessible, Ngrok is integrated, enabling secure tunneling for real-time testing, external API calls, and sharing the application without deployment.
